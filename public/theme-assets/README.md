
  # Use image for theme

  This is a code bundle for Use image for theme. The original project is available at https://www.figma.com/design/TqjUITqUsmjzwIpD2bM1pg/Use-image-for-theme.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  